services:
  telegent:
    build: .
    env_file: .env
    # Stateless bridge — no volume, no database (the brain owns memory).
    # Set BRAIN_URL in .env to reach the running universal-chat-agent.
    restart: unless-stopped
