# data_repositories — placeholder (A-Boxed L1 full skeleton)

Intentionally empty — **the bridge is stateless by design.** Conversation
memory lives in the brain (`universal-chat-agent`), keyed by `conversation_id`.
Keeping the bridge stateless is what lets many bridges (Telegram, WhatsApp, …)
share one brain and one memory without coordination.
