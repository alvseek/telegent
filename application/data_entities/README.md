# data_entities — placeholder (A-Boxed L1 full skeleton)

Intentionally empty — the bridge persists nothing (see `data_repositories/`).
Persisted shapes live in the brain (`universal-chat-agent/data_entities`).
