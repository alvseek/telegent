# business_domain — placeholder (A-Boxed L1 full skeleton)

Intentionally empty. The bridge has no domain rules of its own — conversation
rules live in the **brain** (`universal-chat-agent/business_domain`). The one
pure helper the bridge does have (message chunking) is a generic utility, so it
lives in `common/`, not here.
